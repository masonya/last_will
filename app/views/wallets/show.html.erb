<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @wallet.title %>
</p>

<p>
  <strong>Body:</strong>
  <%= @wallet.body %>
</p>

<%= link_to 'Edit', edit_wallet_path(@wallet) %> |
<%= link_to 'Back', wallets_path %>
